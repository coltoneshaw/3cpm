type pageIds = 'summary-stats' | 'risk-monitor' | 'performance-monitor' ;
type pageNames = 'Summary Statistics' | 'Risk Monitor' | 'Performance Monitor';

type buttonElements = {
    name: pageNames,
    id: pageIds
}[]