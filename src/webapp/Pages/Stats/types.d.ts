export type PageIds = 'summary-stats' | 'risk-monitor' | 'performance-monitor';
export type PageNames = 'Summary Statistics' | 'Risk Monitor' | 'Performance Monitor';

export type ButtonElements = {
  name: PageNames,
  id: PageIds
}[];
